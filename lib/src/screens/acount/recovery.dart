import 'package:flutter/material.dart';

class Recovery extends StatelessWidget {
  static const routeName = 'root/login/recovery';

  const Recovery({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
