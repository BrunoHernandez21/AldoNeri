import 'package:flutter/material.dart';

class InformacionLegal extends StatelessWidget {
  const InformacionLegal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
