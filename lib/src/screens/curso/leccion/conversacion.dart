import 'package:flutter/material.dart';

class Conversacion extends StatelessWidget {
  const Conversacion({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Conversacion'),
    );
  }
}
